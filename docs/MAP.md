# MAP — Architectural Map

Status: **stub**. To be filled in Phase 4 (map) after Deploy 1 completes.

Will contain: module dependency graph · request flow (client → server → tool → db → response) · integration points (Fly, HF Hub, GoDaddy DNS, Let's Encrypt, PyPI, GitHub Actions) · hot-spots (most-called tools, largest tables) · «لا تلمس» zone.
